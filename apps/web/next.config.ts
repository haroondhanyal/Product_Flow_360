import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Hide the Next.js development "N" overlay from the product workspace UI.
  devIndicators: false,
};

export default nextConfig;
