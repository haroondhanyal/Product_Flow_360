JWT_SECRET=replace-with-a-random-secret-of-at-least-32-characters
WEB_ORIGIN=http://localhost:3000
PORT=4000
# Optional: defaults to apps/api/.data/pf360.json when the API is started.
# LOCAL_DATA_FILE=.data/pf360.json
